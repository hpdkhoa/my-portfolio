{
  "_readme": "Fill any value you have real measurements for; leave others null or empty. Run tools/fill_portfolio.py afterwards — it renders ONLY non-empty values into the writeups, so a partially filled file is fine. Never invent numbers: every value here should come from your benchmark harness or a real run.",

  "hieuluat": {
    "corpus_documents": null,
    "corpus_chunks": null,

    "index_comparison": {
      "_columns": ["index", "recall_at_10", "p50_latency_ms", "p95_latency_ms"],
      "rows": []
    },
    "_index_example": [["sequential scan", 1.00, 240, 410], ["IVFFlat (lists=100)", 0.97, 12, 28], ["HNSW (m=16)", 0.99, 8, 19]],

    "rerank_effect": {
      "_columns": ["config", "answer_quality_pct", "added_latency_ms"],
      "rows": []
    },
    "_rerank_example": [["retrieve only (k=10)", 78, 0], ["retrieve k=50 + cross-encoder rerank", 91, 85]],

    "embedding_throughput": {
      "_columns": ["precision", "docs_per_sec_bulk", "vram_gb"],
      "rows": []
    }
  },

  "gen_system": {
    "offload_curve": {
      "_columns": ["gpu_layers", "tokens_per_sec", "vram_gb"],
      "rows": []
    },
    "streaming": {
      "_columns": ["config", "ttft_ms", "total_latency_s"],
      "rows": []
    },
    "quantization_sweep": {
      "_columns": ["quant", "tokens_per_sec", "vram_gb", "compile_pass_rate_pct"],
      "rows": []
    },
    "two_model_strategies": {
      "_columns": ["strategy", "reload_cost_s", "tokens_per_sec", "compile_pass_rate_pct"],
      "rows": []
    }
  }
}
